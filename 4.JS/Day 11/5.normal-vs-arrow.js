function mnakeUpperCase(input){
   return input.toUpperCase();
}

const makeUpperCase2 = (input) => input. toUpperCase();


const makeUpperCaseAndTrim = (input) =>{

const res = makeUpperCase2(input);
return res.trim();

} 