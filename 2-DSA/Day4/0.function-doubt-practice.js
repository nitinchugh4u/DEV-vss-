
function add(x,y){
    return x + y;
}
 let add  = add(10,20);  // It will show  we cant decleared same variable name as the function name.

 var add = add(10,20)  // but it will print the value 
 console.log(add);